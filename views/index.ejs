<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Backend</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
   <div class="w-full min-h-screen bg-zinc-900" >
<div class="form p-10 text-white" >
    <form action="/create" method="post" >
<input name="title" class="block outline-none  w-full rounded-md py-2 px-3 bg-zinc-800 " type="text" placeholder="Title goes here" >
<textarea name="details" class="block outline-none w-full rounded-md mt-2 py-2 px-3 bg-zinc-800 resize-none"  placeholder="write your task details" ></textarea>    
<input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Submit" >
</form>
</div>
<div class="tasks flex gap-3 flex-wrap p-10" >
   <% if(files.length > 0){ %>
    <% files.forEach(function(val){ %>
        <div class="task w-72 rounded-md px-3 py-4 bg-zinc-800" >
            <h1 class="text-white text-2xl tracking-tighter" ><%= val%></h1>
            <a class="text-blue-500 inline-block mt-3" href="/file/<%=val%>" >Read More</a>  
            </div>
   <% }) %>
  <% } else { %>
        <h3 class="text-zinc-400" >No tasks yet.</h3>
    <% } %>
    
</div>
   </div>
</body>
</html>